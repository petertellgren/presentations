var config = {
	// store default list of presentations
	presentations : {
		'sass-less' : { // demo presentation
			id: 'sass-less',  // id for each presentation, currently same as the url
			title : 'Sass vs. LESS',
			indexh : 0,  // initial slide horizontal index
			indexv : 0,  // initial slide veriticlal index
		}
	}

};

exports.config = config;
